(function() {
    angular
        .module("PaivConfig", [
            // "ngInject", 
            "ngAnimate" // ngAnimate module supports both CSS-based and JS-based animations via callback hooks
            , "ngMaterial", "ngMessages", "ngAria"

        ]);
})();